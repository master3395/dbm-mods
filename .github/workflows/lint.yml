name: Lint

on:
  pull_request:
  push:
    branches:
      - master
    paths:
      - actions
      - events
      - extensions
      - sharder
      - bot.js
      - bot.ts
      - types.ts

jobs:
  lint:
    name: ESLint (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # LTS anchors; omit Node 26 until actions/setup-node publishes linux x64 builds in its version manifest.
        node-version: [22, 24]

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          cache: npm
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint (JS + TS)
        run: npm run lint:all

      - name: Run Prettier
        run: npm run prettier:check
